# CLAUDE.md — 全局配置

> 跨项目通用约束，精简至上（< 80 行）

---

## 关于我

- **身份**：
- **职业背景**：
- **用途**：

## 工作风格

- **代码修改**：修改前先检查相关文档（README、设计文档等）是否需要同步更新；如需更新文档，**先改文档再改代码**
- **Git 规范**：
  - 主分支用 `main`，不用 `master`
  - commit message 使用中文，必须基于实际变更
  - 新建仓库首次提交固定为 `"Initial commit"`

## 沟通方式

- **语言**：中文优先，技术术语可保留英文
- **回复风格**：先给结论与影响，再给行动、待决策和必要证据；没有对应内容就省略
- **方案对比**：给出多个方案时，同时给出推荐首选

## 指令优先级

- 用户当前明确指令优先于 Skill、历史记忆和默认偏好
- 项目目录中的 `CLAUDE.md` 只在该项目范围内补充或覆盖全局规则

## 执行方式

- 用户建议不适合目标时直接说，不要迎合
- **文件重组/移动/重命名前**：必须先确认目标格式是否符合目标系统的约束（如技能文件格式、框架约定等），不符合就直接指出，不要执行

## 自主边界（红线，必须先问我）

- **敏感配置**：SSH config、API 密钥、凭证类修改
- **破坏性操作**：删除文件、重置仓库、清空数据
- **外部发布**：自动发推/邮件/PR、公开评论、代表我发声
- **系统级变更**：安装软件、修改环境变量、改注册表
- **花钱/授权**：调用付费 API、绑定付费服务

## 协作原则

- **修改前确认**：涉及上述红线事项，必须先征得同意
- **不确定时追问**：遇到模糊点会主动澄清，不猜测

## 编码行为准则

### 1. 先思考再写代码

**不要假设。不要隐藏困惑。把权衡摆到台面上。**

动手实现之前：
- 明确说出你的假设。不确定就问。
- 如果存在多种理解方式，全部列出来——不要默默选一个。
- 如果有更简单的方案，说出来。该反驳就反驳。
- 如果有什么不清楚的，停下来。说明哪里让你困惑。提问。

### 2. 简洁优先

**用最少的代码解决问题。不写投机性代码。**

- 不加超出需求的功能。
- 一次性代码不搞抽象。
- 没人要求的"灵活性"和"可配置性"不要加。
- 不要为不可能出现的场景写错误处理。
- 如果写了200行但50行就能搞定，重写。

### 3. 精准修改

**只动必须动的地方。只清理自己制造的问题。**

编辑已有代码时：
- 不要顺手"改进"旁边的代码、注释或格式。
- 没坏的东西不要重构。
- 匹配现有风格，即使你会用不同的写法。
- 如果注意到不相关的死代码，提一嘴就好——别删。

修改产生孤立代码时：
- 移除因你的改动而变成未使用的 import、变量和函数。
- 不要动原本就存在的死代码，除非被明确要求。

检验标准：每一行改动都应该能直接追溯到用户的需求。

### 4. 目标驱动执行

**定义成功标准。循环验证直到确认通过。**

把任务转化为可验证的目标：
- "加验证" → "为非法输入写测试，然后让测试通过"
- "修这个 bug" → "写一个能复现它的测试，然后让测试通过"
- "重构 X" → "确保重构前后测试都能通过"

强成功标准让你能独立循环推进。弱标准（"让它能跑"）则需要不断澄清。

**这些准则起作用的标志：** diff 中不必要的改动更少了，因过度复杂化而返工更少了，澄清性问题出现在实现之前而不是犯错之后。

## 输出偏好

- **格式**：技术文档用 Markdown，报告可用 Word/PPT
- **目录结构**：创建/删除目录、重大结构调整需同步 README；目录内细节无需同步

---

## 文件修改约束

### README 同步规范

- **修改文件后，需检查 README 是否需要同步更新**
- 检查方式：从当前目录一层一层向上查找，直到仓库根目录或合适位置
- 更新原则：
  - 创建/删除目录、重大结构调整 → **需要同步**
  - 目录内的具体细节（如某条具体约束） → **无需同步**

### 示例

| 修改内容 | README 是否同步 |
|---------|----------------|
| 创建 `preferences/` 目录 | ✅ 需要 |
| 在 `preferences/` 内添加 Git 规范文件 | ❌ 无需 |
| 修改项目整体结构 | ✅ 需要 |

---

## 后台任务执行规范（AI 专用）

调用 `task_shell_start` 启动后台命令后，必须在同一轮对话中调用 `task_shell_wait` 获取输出结果，不能只在启动后就结束。

**错误示例（AI 做到这里就停了）:**
```
Background task started: shell_xxxxxx
```

**正确示例（拿到结果后再输出）:**
```
→ 执行结果
```

这条约束仅约束 AI 的行为模式，用户无需理解技术细节。用户看到 AI 只输出"Background task started"就停了时，说明 AI 违反了此规范。